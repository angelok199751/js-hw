const salary = 1000;
let grade;
if (salary >= 1000) {
    grade = "middle"; // Если salary больше или равно 1000, то в консоль выведет "middle"
} else {
    grade = "junior"; // Если salary меньше 1000, то в консоль выведет "junior"
}
console.log(grade);